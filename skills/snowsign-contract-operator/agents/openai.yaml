interface:
  display_name: "SnowSign Contract Operator"
  short_description: "SnowSign 일반 계약과 링크서명 업무 처리"
  default_prompt: "Use $snowsign-contract-operator to find completed contracts, including link-signing contracts, and summarize the results."

policy:
  allow_implicit_invocation: true
