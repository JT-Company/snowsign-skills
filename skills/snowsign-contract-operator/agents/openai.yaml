interface:
  display_name: "SnowSign Contract Operator"
  short_description: "SnowSign 계약 생성·발송·조회·다운로드 운영"
  default_prompt: "Use $snowsign-contract-operator to safely create, send, inspect, or download a SnowSign contract."

policy:
  allow_implicit_invocation: true
